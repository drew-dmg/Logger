import { PagePlaceholder } from './PagePlaceholder';

export function HistoryPage() {
  return <PagePlaceholder title="History" description="Browse past entries and identify repeating symptom patterns over time." />;
}
